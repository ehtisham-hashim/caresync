## 2024-05-20 - Adding ARIA labels to icon-only buttons
**Learning:** Icon-only buttons, such as a mobile menu toggle, present accessibility challenges for screen readers unless specifically described. Context-aware `aria-label` and `aria-expanded` attributes greatly improve the usability.
**Action:** Always identify icon-only interactive elements and ensure they have adequate descriptive attributes mapping to their dynamic state.
